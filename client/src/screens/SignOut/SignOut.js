import { Layout } from '../../components'

const SignOut = (props) => {

  return (
    <Layout user={props.user}>
      <div>
        <h1>Sign Out Page</h1>
      </div>
    </Layout>
  )
}

export default SignOut
